/** 
    Handles coordinating events across the application
    @module app/mods/vent
    @extends Backbone.Events
 */
define(['backbone'], 
function(Backbone){
    return _.clone(Backbone.Events);
});